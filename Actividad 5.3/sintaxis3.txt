prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}

prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}

prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}
prueba.cpp

#   include<iostream>
#   include "string"
using namespace std;

int   main () {
    int a = 2, b,c ;
    bool _matrix ;// Error de sintaxis
    int r[3]; // Array of three elements
    int m[2][2]={{8,4},{0,1}} // Matriz de 2x2 elementos

    for  ( int   i  =  0 ;  i  <  n ; i++ ) {
        b = 1 + 60E10 - i ;
        c = a ^ + b * 75.6 ;
        cout<<c<<endl;
    }

    return 0;
}